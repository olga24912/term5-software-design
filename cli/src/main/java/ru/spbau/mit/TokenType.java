package ru.spbau.mit;

// It is all types of tokens in expression.
public enum TokenType {
    TokenVariable, TokenPipe, TokenCommand, TokenText, TokenAssign,
    TokenDoubleQuote, TokenQuote, TokenSpace, TokenVariableName
}
